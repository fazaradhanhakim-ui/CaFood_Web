{
  "name": "cafood-web",
  "version": "1.0.0",
  "description": "CaFood - Cafe Marketplace Website",
  "main": "index.html",
  "scripts": {
    "start": "npx live-server --port=3000",
    "firebase": "firebase serve",
    "deploy": "firebase deploy"
  },
  "dependencies": {
    "firebase": "^12.12.1"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}